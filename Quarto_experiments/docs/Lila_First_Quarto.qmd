---
title: "Lila's First Quarto"
author: "Lila Goodman"
format: html
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
1 + 1
library(tidyverse)
library(readxl)
myData <- read_xlsx("C:/Users/Owner/OneDrive/Documents/SM/Quarto_experiments/data/sttstj_all_sites.xlsx")
view(myData)
```

I need to learn to make relative paths than direct! how to push to github- commit - go to the gooey (right) or terminal git status git add git commit -m "inital commit and .qmd creation" then push (INTERNET NEEDED) You can add options to executable code like this

get it to github
go to settings of respo
pages change branch to master
change folder to Docs


```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).
